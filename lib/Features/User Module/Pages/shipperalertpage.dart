import 'package:flutter/material.dart';

class ShipperAlertPage extends StatelessWidget {
  const ShipperAlertPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}