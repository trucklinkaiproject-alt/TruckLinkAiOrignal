import 'package:flutter/material.dart';

class ShipperOrderPage extends StatelessWidget {
  const ShipperOrderPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}